---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-03

must_haves:
  truths:
    - "https://nvtran61091.github.io loads in a browser and shows the Math Adventure start screen"
    - "The browser console shows zero errors when the GitHub Pages URL loads"
    - "The page renders correctly on a real mobile browser (not just DevTools emulation)"
  artifacts:
    - path: "index.html"
      provides: "Deployed game file at GitHub Pages root"
      contains: "Math Adventure"
  key_links:
    - from: "git push origin main"
      to: "https://nvtran61091.github.io"
      via: "GitHub Pages auto-deploy from repo root"
      pattern: "nvtran61091\\.github\\.io"
---

<objective>
Deploy the completed `index.html` to GitHub Pages and verify the live URL serves the game correctly. This validates the entire deployment path (DEPLOY-03) — the last requirement for Phase 1 Foundation.

Purpose: Confirm that the single-file deployment model works end-to-end before any game logic is built. If GitHub Pages has issues (wrong branch, wrong root, caching), discover that now rather than at Phase 4.

Output: Live game accessible at `https://nvtran61091.github.io` with no errors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<!-- Deployment facts (from 01-RESEARCH.md Pattern 4): -->
<!-- Repo: nvtran61091.github.io — username.github.io naming auto-enables GitHub Pages -->
<!-- Branch: main (default for username.github.io repos) -->
<!-- File: /index.html at repo root -->
<!-- No GitHub Actions, no Jekyll, no _config.yml needed -->
<!-- Propagation: ~30–60 seconds after push -->
<!-- CDN note: hard-reload (Ctrl+Shift+R) to bypass local cache -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push index.html to main branch</name>
  <files></files>
  <read_first>
    - index.html (confirm it exists and is not the original "hello" placeholder)
  </read_first>
  <action>
Run these git commands from the repository root (`D:\My-Project\nvtran61091.github.io`):

```bash
git add index.html
git commit -m "feat(01-foundation): complete static prototype with puzzle generator and all 4 screens"
git push origin main
```

**Before running:** Confirm `index.html` contains `Math Adventure` (not the original `hello`) by checking the first 10 lines or grepping for the title. If the file still says "hello", Plan 01 did not complete — do not push.

**After push:** Note the commit hash. GitHub Pages will auto-deploy within 30–60 seconds. No GitHub Actions, no workflow files, and no `_config.yml` are needed — `nvtran61091.github.io` repos auto-serve `index.html` from the repo root.

**If push is rejected** (non-fast-forward): Run `git pull --rebase origin main` first, then `git push origin main`.

**If git is not initialized:** Run `git init && git remote add origin https://github.com/nvtran61091/nvtran61091.github.io.git` first. (Unlikely — repo already exists per project context.)
  </action>
  <verify>
    <automated>
# Confirm the commit exists and contains the right file
git --no-pager log --oneline -3
git --no-pager show HEAD --stat
# Expected output: commit hash, message contains "01-foundation", index.html in changed files
    </automated>
  </verify>
  <acceptance_criteria>
- `git log --oneline -1` shows a commit with "feat(01-foundation)" in the message
- `git show HEAD --stat` lists `index.html` as a changed file
- `git push` exits with code 0 (no rejected push errors)
  </acceptance_criteria>
  <done>index.html is committed and pushed to main. git log confirms the commit. No push errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live GitHub Pages deployment — game loads with no errors</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation/01-01-SUMMARY.md (what was built)
  </read_first>
  <what-built>
    index.html pushed to main branch of nvtran61091.github.io repo. GitHub Pages auto-serves the file at https://nvtran61091.github.io.
  </what-built>
  <how-to-verify>
**Step 1 — Wait for deployment:**
Wait 60 seconds after the push completes. GitHub Pages propagation is typically 30–60 seconds for a `username.github.io` repo.

If you want to confirm deployment before opening the URL: go to `https://github.com/nvtran61091/nvtran61091.github.io/actions` — look for a "pages build and deployment" workflow run. Wait for the green checkmark.

**Step 2 — Hard-reload the live URL:**
Open `https://nvtran61091.github.io` in Chrome. Press **Ctrl+Shift+R** (Windows/Linux) or **Cmd+Shift+R** (Mac) to bypass CDN cache. If the old "hello" page still appears, wait another 2–3 minutes and hard-reload again — CDN propagation can take up to 10 minutes.

**Step 3 — Verify the start screen loads:**
Expected: warm-yellow background, white game card, 🔢 emoji, "Math Adventure!" heading in rounded font, orange "▶ Play!" button. No blank page, no "hello", no 404.

**Step 4 — Check for console errors:**
Open DevTools (F12) → Console tab. Confirm: zero red errors. Warnings are acceptable (e.g., favicon 404). Any JavaScript error = bug to report.

**Step 5 — Run puzzle validation on live URL:**
With the live page open, paste this in the DevTools console:
```javascript
const p = PuzzleGenerator.generate(10);
console.assert(p.length === 10, 'FAIL: not 10 puzzles');
console.assert(p.every(x => x.answer > 0 && x.answer < 20), 'FAIL: answer out of range');
console.assert(new Set(p.map(x=>`${x.operandA}${x.operator}${x.operandB}`)).size === 10, 'FAIL: duplicates');
console.log('%c✓ Live puzzle generation valid', 'color:green;font-weight:bold');
```
Expected: Green ✓ message. No red assertion errors.

**Step 6 — Mobile device check (optional but recommended):**
Open `https://nvtran61091.github.io` on a real phone (not DevTools emulation). Confirm: game card fills the screen, fonts are large and readable, ▶ Play! button is easy to tap.

**Known limitation:** If the site still shows the old "hello" after 10 minutes, check GitHub Pages settings at `https://github.com/nvtran61091/nvtran61091.github.io/settings/pages` — confirm Source is set to "Deploy from branch: main / (root)".
  </how-to-verify>
  <acceptance_criteria>
- https://nvtran61091.github.io loads the Math Adventure start screen (not "hello", not 404)
- Zero JavaScript errors in the browser console
- Puzzle validation script passes with green ✓ on the live URL
- Page is visually correct: yellow background, white card, emoji, orange button
  </acceptance_criteria>
  <resume-signal>Type "deployed" if the live URL shows the game with no errors. Describe any issues (still showing "hello", 404, JS errors) so they can be diagnosed.</resume-signal>
  <action>Human verification task — navigate to https://nvtran61091.github.io (hard-reload with Ctrl+Shift+R), verify start screen loads, check DevTools console for errors, run console puzzle validation script.</action>
  <verify>
    <automated>MISSING — live URL verification requires human browser access; no automated substitute for external URL check</automated>
  </verify>
  <done>https://nvtran61091.github.io shows Math Adventure start screen with zero console errors. Puzzle validation passes on live URL.</done>
</task>

</tasks>

<verification>
- `git log --oneline -1` shows the foundation commit
- Live URL `https://nvtran61091.github.io` shows the game start screen
- Console puzzle validation passes on live URL
- Zero JS errors on live URL
</verification>

<success_criteria>
- index.html is live at https://nvtran61091.github.io
- Game start screen renders with correct visual design
- PuzzleGenerator works correctly in the deployed environment
- No errors in browser console on the live URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Commit hash and commit message
- Time from push to live deployment (seconds)
- Any deployment issues encountered and how they were resolved
- Live URL confirmation: pass/fail
- Console puzzle test result on live URL: pass/fail
</output>
